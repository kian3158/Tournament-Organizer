# Screenshots

Drop UI captures here, then reference them from the top-level `README.md`.

Suggested shots:

- `bracket-dark.png` — a generated bracket in dark mode
- `standings-light.png` — round-robin standings in light mode

Keep them reasonably sized (PNG, ~1200px wide is plenty).
